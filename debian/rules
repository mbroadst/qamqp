#!/usr/bin/make -f
# -*- makefile -*-
# Sample debian/rules that uses debhelper.
# This file was originally written by Joey Hess and Craig Small.
# As a special exception, when this file is copied by dh-make into a
# dh-make output file, you may use that output file without restriction.
# This special exception was added by Craig Small in version 0.37 of dh-make.

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ 

# The `make check` step currently fails on Debian Wheezy
override_dh_auto_test:
	true

# `make install` winds up putting everything in debian/tmp, we need to
# copy an appropriate subset into the other debian/ package directories.
override_dh_auto_install:
	dh_auto_install
	( cd debian/tmp ; tar cf - usr/lib*/lib*.so* ) \
		| ( cd debian/libqamqp ; tar xf - )
	( cd debian/tmp ; tar cf - usr/include usr/lib*/pkgconfig ) \
		| ( cd debian/libqamqp-dev ; tar xf - )
